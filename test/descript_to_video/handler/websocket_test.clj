(ns descript-to-video.handler.websocket-test
  (:require [descript-to-video.handler.websocket :refer :all]))

(deftest descript-to-video.handler.websocket-test 
  (testing "websocket-handlerのテスト"))

(comment 
  (start )
  (start {:exo-path "./test/resources/test_sample.exo"
           :markdown-path "./test/resources/test_sample.md"})

  )